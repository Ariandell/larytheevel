# Add the newly generated Gemini API key here locally, never in browser code.
GEMINI_API_KEY=

# The server accepts at most this many successful images before it stops.
# On a public serverless deployment, use a persistent database for a global count.
GENERATION_LIMIT=1000
